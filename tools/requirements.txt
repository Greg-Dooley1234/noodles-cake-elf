# python3 -m pip install --user -r tools/requirements.txt
pillow
numpy
scipy
soundfile      # decodes the .ogg voice notes
lameenc        # mp3 encoding for the embedded clips
av             # reads the reference clip already inside index.html
faster-whisper # transcribes voice takes so you can pick in/out points
